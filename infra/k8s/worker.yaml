apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-{{APP_ENV}}
  namespace: {{APP_ENV}}
  labels:
    app: worker-{{APP_ENV}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker-{{APP_ENV}}
  template:
    metadata:
      labels:
        app: worker-{{APP_ENV}}
    spec:
      containers:
        - name: worker-{{APP_ENV}}
          image: {{WORKER_IMAGE}}
